# G.F.B.S.-Mirage 贡献指南

感谢你愿意为本仓库做贡献。本指南说明如何提 Issue、提交代码、发起 Pull Request，以及代码风格与协作约定。

## 适用范围

你可以通过以下方式贡献：

* 报告 Bug / 提交复现步骤
* 提出功能建议 / 设计讨论
* 改进文档、注释、示例
* 提交修复、重构、性能优化
* 增加测试或验证用例

## 行为准则

请保持尊重、理性沟通。避免人身攻击、引战、刷屏。维护者有权关闭不符合规范的 Issue/PR。

## 提交前准备

1. Fork 本仓库到你的账号下
2. 克隆你的 Fork 到本地
3. 新建分支进行开发（不要直接在 main/master 上改）
4. 尽量保持每个 PR 只解决一个问题，范围越小越容易合并

分支命名建议：

* feature/xxx（新功能）
* fix/xxx（修复）
* refactor/xxx（重构）

## 如何提交 Issue

请在 Issues 中提交，并尽量包含以下信息：

* 标题：一句话说明问题/需求
* 现象描述：你看到了什么
* 期望结果：你希望发生什么
* 复现步骤：越具体越好（含配置/指令/操作顺序）
* 环境信息：Minecraft 版本、Forge/Fabric（如适用）、Java 版本、操作系统、服务端/客户端
* 日志与截图：关键报错栈、崩溃报告、latest.log 等（请粘贴相关片段，不要只丢一句“崩了”）

不建议把“功能讨论 + 大量实现细节”直接塞进一个 Issue。复杂变更请先开讨论 Issue，达成一致后再写 PR。

## 开发与代码规范

* 遵循项目现有结构与风格，不要引入风格大变动
* 保持格式一致（缩进、命名、文件组织）
* 不要把无关格式化（全仓库 reformat）混入功能提交
* 尽量避免不必要的依赖新增；确需新增请在 PR 说明原因与替代方案
* 与 Minecraft/模组相关的改动要说明兼容性影响（版本、配置、数据包、存档等）

## 测试与验证

提交前请至少完成：

* 能编译通过
* 基本功能自测通过（说明你怎么测的：单机/服务器、测试场景）
* 修复类 PR 请提供复现与修复验证说明
* 新功能请提供最小可用验证步骤或示例配置

如果仓库已有 CI/测试流程，请确保它们通过。

## 提交信息（Commit Message）规范

建议使用统一格式，便于生成变更记录：

类型(范围): 简短描述

常用类型：

* feat：新增功能
* fix：修复问题
* refactor：重构

示例：

* fix(network): 修复断线后重复注册导致崩溃
* feat(config): 新增服务器侧开关以控制广播频率

## Pull Request（PR）流程

1. 确认你基于最新的主分支（main/master）进行开发（必要时 rebase）
2. 推送你的分支到 Fork
3. 在 GitHub 上发起 PR，目标分支选择仓库默认分支
4. PR 描述请包含：

   * 改动内容概述（做了什么）
   * 改动原因（为什么做）
   * 影响范围（是否破坏兼容、是否需要迁移/改配置）
   * 验证方式（你怎么测的）
   * 关联 Issue（用 #编号）

维护者可能会要求你：

* 调整实现方式
* 补充测试/文档
* 拆分 PR 或缩小范围
  请按评论更新代码并推送到同一分支，PR 会自动更新。

## 不会被接受的常见情况

* 大范围无关格式化/重命名
* 没有说明、没有验证、没有复现步骤
* 把多个无关改动混在一个 PR
* 引入明显不必要的依赖或把临时代码/调试代码提交进来
* 破坏兼容性但不说明迁移方式

## 许可与署名

提交代码即表示你同意：你的贡献将按本仓库采用的开源许可协议进行发布与再分发。若仓库存在额外的版权声明或 CLA 规则，以仓库说明为准。

---

- 本文由ChatGPT编写。
